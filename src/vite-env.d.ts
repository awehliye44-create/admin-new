/// <reference types="vite/client" />

// Google Maps types - loaded via @types/google.maps
// Declare the global google namespace so TypeScript recognizes it
declare namespace google.maps {
  // The actual types are provided by @types/google.maps at runtime
  // This declaration ensures TypeScript recognizes the namespace
}
