name: Deploy Admin Panel

# Clean deploy path: GitHub Actions Pages (workflow), not legacy gh-pages branch builds.
# Legacy branch builds were racing/cancelling (pages-build-deployment) and leaving
# adminonecab.net stuck on an old CDN asset.

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: admin-pages
  # Never cancel an in-flight Pages deploy — that left the site on a stale asset.
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Finance recovery SSOT guard
        run: bash scripts/check-finance-recovery-ssot.sh

      - name: Finance payout SSOT parity guard
        run: bash scripts/check-finance-payout-ssot-parity.sh

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Build
        run: npm run build
        env:
          # Apex custom domain adminonecab.net — site root (not /admin-new/)
          GITHUB_PAGES: ""
          VITE_SUPABASE_URL: https://thazislrdkjpvvghtvzo.supabase.co
          VITE_SUPABASE_PUBLISHABLE_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InRoYXppc2xyZGtqcHZ2Z2h0dnpvIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Njc4NzA1MjIsImV4cCI6MjA4MzQ0NjUyMn0.pXaycIz1t7JXuItyqvjNNrFsZpsaXbB5bV1OWSQLbWM
          VITE_SUPABASE_PROJECT_ID: thazislrdkjpvvghtvzo

      - name: SPA fallback + custom domain + nojekyll
        run: |
          cp dist/index.html dist/404.html
          printf 'adminonecab.net\n' > dist/CNAME
          touch dist/.nojekyll
          test -f dist/auth/revolut/callback/index.html
          # Record source SHA for host verification
          printf '%s\n' "${{ github.sha }}" > dist/deploy-source-commit.txt

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
